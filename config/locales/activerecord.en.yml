en:
  activerecord:
    attributes:
      user:
        first_name: First name
  errors:
    models:
      user:
        attributes:
          first_name:
            blank: can't be blank
  models:
    user:
      one: User
      other: Users